<% if (!user) { %>
  <link rel="stylesheet" href="/stylesheets/siwg.css">
  <% } %>
    </head>

    <body>
      <nav>
        <a class="nav-button" href="/recipes">Recipes</a>
        <a class="nav-button" href="/profiles">Profiles</a>
        <% if (user) { %>
          <a class="nav-button" href="/auth/logout"> Logout</a>
          <% } else { %>
            <a class="nav-button-google" href="/auth/google" class="siwg-link">
              <button type="submit" class="siwg">
                <img id="siwg-image" alt="Sign in with Google" />
              </button> 
            </a>
            <% } %>
      </nav>